// Package codexresponses calls the subscription-backed Codex Responses endpoint.
//
// The endpoint is an unsupported ChatGPT backend contract. This package is the
// only place that contract may leak, so a provider change has one repair site.
package codexresponses
